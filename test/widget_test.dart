// Placeholder kept so `flutter test` has something to run.
// Real widget tests live alongside features; see streak_calculator_test.dart
// for an example of pure-logic unit testing.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
